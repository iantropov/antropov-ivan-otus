package main

import "fmt"

func main() {
	// for i := 1; i <= 5; i++ {
	// 	fmt.Printf("There are %d happy tickets with %d digits.\n", FindHappyTicketsAsSenior(i), i)
	// }

	// for i := 6; i <= 8; i++ {
	// 	fmt.Printf("There are %d happy tickets with %d digits.\n", FindHappyTicketsAsSenior(i), i)
	// }

	for i := 9; i <= 9; i++ {
		fmt.Printf("There are %d happy tickets with %d digits.\n", FindHappyTicketsAsSenior(i), i)
	}
}
